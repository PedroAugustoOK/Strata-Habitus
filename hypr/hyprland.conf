################
### MONITORS ###
################
monitor = eDP-1, 1920x1080@60, 0x0, 1
monitor = HDMI-A-1, 1920x1080@144, 0x0, 1, mirror, eDP-1

###################
### MY PROGRAMS ###
###################
$terminal    = kitty
$fileManager = nautilus
$mainMod     = SUPER

#################
### AUTOSTART ###
#################
#exec-once = quickshell
exec-once = swww-daemon
exec-once = hypridle
exec-once = QT_WAYLAND_DISABLE_WINDOWDECORATION=1 quickshell

#############################
### ENVIRONMENT VARIABLES ###
#############################
env = XCURSOR_SIZE, 24
env = HYPRCURSOR_SIZE, 24
env = NIXOS_OZONE_WL, 1
env = QT_QPA_PLATFORM, wayland
env = GDK_BACKEND, wayland,x11
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = KITTY_CONFIG_DIRECTORY, /home/ankh/dotfiles/kitty

#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in  = 2
    gaps_out = 5, 15, 15, 15
    border_size = 1
    #col.active_border   = rgba(00000000)
    #col.inactive_border = rgba(00000000)
}

decoration {
    rounding = 12
}

animations {
    enabled = true

    # Beziers
    # Entrada com overshoot suave (bounce na chegada)
    bezier = wind,        0.05, 0.9,  0.1,  1.05
    # Entrada mais pronunciada — leve elástico
    bezier = winIn,       0.1,  1.1,  0.1,  1.1
    # Saída rápida — sai para baixo e some
    bezier = winOut,      0.3, -0.3,  0,    1
    # Suave cubic para movimentos
    bezier = smoothMove,  0.25, 1,    0.5,  1
    # Workspaces: quase linear mas com aceleração inicial
    bezier = swipe,       0.3,  0.9,  0.2,  1
    # Fade rápido
    bezier = quick,       0.15, 0,    0.1,  1

    # Janelas
    animation = windows,       1, 5,   wind,       slide
    animation = windowsIn,     1, 5,   winIn,      slide
    animation = windowsOut,    1, 4,   winOut,     slide
    animation = windowsMove,   1, 4,   smoothMove, slide

    # Fade (opacidade)
    animation = fade,          1, 3,   quick
    animation = fadeIn,        1, 4,   winIn
    animation = fadeOut,       1, 3,   quick

    # Layers (overlays do Quickshell — launcher, CC, powermenu)
    # Usam fade puro para não conflitar com as animações QML internas
    animation = layers,        1, 3,   quick,      fade
    animation = layersIn,      1, 4,   winIn,      fade
    animation = layersOut,     1, 2,   quick,      fade

    # Border
    animation = border,        1, 5,   wind

    # Workspaces — slide lateral fluido
    animation = workspaces,    1, 4,   swipe,      slide
    animation = specialWorkspace, 1, 4, swipe,     slidevert
}

dwindle {
    pseudotile     = true
    preserve_split = true
}

#############
### INPUT ###
#############
input {
    kb_layout  = br
    kb_model   = abnt2
    follow_mouse = 1
    sensitivity  = 0

    touchpad {
        natural_scroll = true
    }
}

gesture = 3, horizontal, workspace

#####################
### KEYBINDINGS   ###
#####################

# Apps
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, F,      exec, $fileManager
bind = $mainMod, W,      killactive
bind = $mainMod, M,      exit
bind = $mainMod, V,      togglefloating
bind = $mainMod, T,      fullscreen, 0
bind = $mainMod, Space, exec, quickshell ipc call launcher toggle
bind = $mainMod, Escape, exec, quickshell ipc call powermenu toggle

# Screenshots
bind  = , Print,       exec, grimblast copy area
bind  = SHIFT, Print,  exec, grimblast copy screen

# Foco
bind = $mainMod, left,  movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up,    movefocus, u
bind = $mainMod, down,  movefocus, d

# Workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Mover janela para workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scratchpad
bind = $mainMod,       S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace,        special:magic

# Scroll entre workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up,   workspace, e-1

# Mover/redimensionar janelas
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Volume / brilho / media
bindel = , XF86AudioRaiseVolume,  exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = , XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = , XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = , XF86MonBrightnessUp,   exec, brightnessctl -e4 -n2 set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-
bindl  = , XF86AudioNext,         exec, playerctl next
bindl  = , XF86AudioPause,        exec, playerctl play-pause
bindl  = , XF86AudioPlay,         exec, playerctl play-pause
bindl  = , XF86AudioPrev,         exec, playerctl previous

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo   = true
    background_color        = 0x111113
}

# File picker flutuante
windowrule = float on, match:class xdg-desktop-portal-gtk
windowrule = size 1200 800, match:class xdg-desktop-portal-gtk
windowrule = move onscreen cursor -50% -50%, match:class xdg-desktop-portal-gtk
windowrule = float on, match:class com.saivert.pwvucontrol
windowrule = float on, match:title impala
windowrule = size 700 500, match:title impala
windowrule = move onscreen cursor -50% -50%, match:title impala
windowrule = float on, match:title bluetui
windowrule = size 1200 700, match:title bluetui
windowrule = move onscreen cursor -50% -50%, match:title bluetui
windowrule = float on, match:class org.gnome.Loupe
windowrule = size 900 700, match:class org.gnome.Loupe
windowrule = move onscreen cursor -50% -50%, match:class org.gnome.Loupe
windowrule = float on, match:class org.pwmt.zathura
windowrule = size 900 700, match:class org.pwmt.zathura
windowrule = move onscreen cursor -50% -50%, match:class org.pwmt.zathura
windowrule = float on, match:class mpv
windowrule = size 1280 720, match:class mpv
windowrule = move onscreen cursor -50% -50%, match:class mpv
windowrule = float on, match:class mpv
windowrule = size 1280 720, match:class mpv
windowrule = move onscreen cursor -50% -50%, match:class mpv
bind = CTRL SUPER, Space, exec, bash ~/.config/quickshell/scripts/theme-switch.sh
bind = SUPER, A, exec, bash /home/ankh/.config/quickshell/scripts/wallpaper-switch.sh
