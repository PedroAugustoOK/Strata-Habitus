# TODO: gerar este arquivo no desktop com:
#   sudo nixos-generate-config --show-hardware-config > ~/dotfiles/hosts/desktop/hardware.nix
#
# O arquivo gerado conterá os módulos de kernel, filesystems e UUIDs
# específicos do hardware da máquina desktop.
{ ... }: { }
